services:
  virdlar:
    build:
      context: .
      args:
        VITE_SUPER_ADMIN_IDS: ${SUPER_ADMIN_IDS}
    env_file: .env
    volumes:
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - public

networks:
  public:
    external: true
    name: jamm-deploy_public
